import styled from "styled-components";

export const OrderTagsWrapper = styled.div`
    display: flex;
    flex-wrap: wrap;
    padding: 4px 0;
    gap: 4px;
    .ant-tag {
        margin: 0;
        line-height: 16px;
        padding: 0 6px;
    }
`;