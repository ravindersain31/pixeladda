import {CaseReducer} from "@reduxjs/toolkit";
import AppState from "@react/editor/redux/reducer/interface.ts";

const refreshPricing: CaseReducer = (state: AppState, action) => {

}

export default refreshPricing;