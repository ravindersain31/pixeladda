<div id="image-progress-modal" class="image-modal hidden">
    <div class="image-modal-box">
        <h3 class="image-modal-title">
            Converting Canvas Designs…
        </h3>

        <p class="image-modal-subtitle">
            Please wait while we convert your designs.
        </p>

        <div class="image-progress-track">
            <div id="image-progress-bar" class="image-progress-bar"></div>
        </div>

        <div id="image-progress-percent" class="image-progress-percent">
            0%
        </div>

        <div id="image-preview-container" style="margin-top: 20px; width: 100%; height: 200px; background: #f5f5f5; border-radius: 8px; overflow: hidden; position: relative; display: flex; align-items: center; justify-content: center;">
            <span style="color: #999; font-size: 12px;">Generating preview...</span>
        </div>
    </div>
</div>

<style>
/* Modal wrapper */
.image-modal {
    position: fixed;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(0, 0, 0, 0.55);
    backdrop-filter: blur(4px);
    z-index: 99999;
}

/* Hidden state */
.image-modal.hidden {
    display: none;
}

/* Modal Box */
.image-modal-box {
    background: #fff;
    width: 420px;
    padding: 28px 30px;
    border-radius: 16px;
    box-shadow:
        0 12px 28px rgba(0,0,0,0.18),
        0 4px 12px rgba(0,0,0,0.12);
    text-align: center;
    animation: scaleIn .25s ease-out;
}

/* Title */
.image-modal-title {
    margin-bottom: 16px;
    font-size: 20px;
    font-weight: 600;
    color: #222;
}

/* Subtitle */
.image-modal-subtitle {
    margin-top: -5px;
    margin-bottom: 18px;
    font-size: 14px;
    color: #666;
}

/* Progress track */
.image-progress-track {
    background: #e6e6e6;
    height: 14px;
    border-radius: 10px;
    overflow: hidden;
    position: relative;
}

/* Progress bar */
.image-progress-bar {
    height: 100%;
    width: 0%;
    background: linear-gradient(90deg, #007bff, #0056d2);
    transition: width .35s ease-out;
}

/* Percentage text */
.image-progress-percent {
    margin-top: 12px;
    font-size: 15px;
    font-weight: 600;
    color: #333;
}

/* Animation */
@keyframes scaleIn {
    0% {
        transform: scale(.85);
        opacity: 0;
    }
    100% {
        transform: scale(1);
        opacity: 1;
    }
}
</style>
