
<div {{ attributes }} class="cart-modal-continer">
    <form data-action="live#action:prevent" data-live-action-param="prevent|save" data-controller="reset-modal-form">
    <div class="modal-header">
        <h1 class="modal-title fs-6 fw-normal" id="saveCartModalLabel">Save Cart</h1>
        <button type="button" class="close-button" data-bs-dismiss="modal" aria-label="Close">×</button>
    </div>
    <div class="modal-body fs-6 fw-normal your-order-quote">
        <p>
             Please complete the fields below to save your order. We will email you a link to your shopping cart.
             You can access your saved design anytime from the <a href="{{ url('my_account') }}">My Account</a> page.
             You may also proceed to checkout and choose See Design - Pay Later.
        </p>
        {% if isSuccessful %}
            <div class="alert alert-{{flashError}} alert-dismissible fade show" role="alert" data-auto-dismiss="5000">
                {{ flashMessage }}
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close">
                    <span aria-hidden="true"></span>
                </button>
            </div>
        {% endif %}

        <div class="mb-3">
			<label for="email" class="form-label required">Email</label>
			<input type="email" id="email" name="email" class="form-control {{ _errors.has('email') ? 'is-invalid' }}" placeholder="Email" data-model="email">
			{% if _errors.has('email') %}
				<div class="invalid-feedback">
					{{ _errors.get('email') }}
				</div>
			{% endif %}
        </div>
        <div class="text-center">
            <button type="submit" data-loading="action(save)|addAttribute(disabled)" class="btn custom-modal-button save-cart-modal-submit">
                <span data-loading="action(save)|show" style="display: none">Saving...</span>
                <span data-loading="action(save)|hide" style="display: block">Save Cart</span>
            </button>
        </div>
    </div>
    </form>
</div>
