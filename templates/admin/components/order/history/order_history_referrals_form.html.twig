
<div {{ attributes }}>
    {{ form_start(form, { attr: {
        'novalidate': true,
        'data-action': 'live#action:prevent',
        'data-live-action-param': 'search'
    } }) }}
    <div class="row">
        <div class="col-12 col-md-9 tab-pane fade show active" id="nav-home" role="tabpanel" aria-labelledby="nav-home-tab" tabindex="0">
            {% set referrals = this.getReferrals() %}
            <div class="row mb-4">
                <div class="col-12">
                    <div class="card shadow-none">
                        <div class="card-body p-0 table-responsive">
                            <table class="table table-bordered mb-0">
                                <thead>
                                    <tr>
                                        <th class="bg-light text-primary" colspan="5">
                                            Report 
                                            [<small class="text-primary">
                                                Date: {{ this.getStartDate()|date('M d, Y') }} - {{ this.getEndDate()|date('M d, Y') }}
                                            </small>]
                                        </th>
                                    </tr>
                                    <tr>
                                        <th>Coupon's Sent</th>
                                        <th>Used Coupons</th>
                                        <th>Given Discount</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <tr>
                                        <td>{{ referrals.totals.requestedCoupons }}</td>
                                        <td>{{ referrals.totals.usedCoupons }}</td>
                                        <td>{{ referrals.totals.discountGiven }}</td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-12 col-md-3 bg-light p-3 rounded">
            {{ form_row(form.startDate) }}
            {{ form_row(form.endDate) }}
            <div class="col-12">
                <button type="submit" data-loading="action(search)|addAttribute(disabled)" class="btn btn-sm btn-primary">
                    <span data-loading="action(search)|show" style="display: none">Searching...</span>
                    <span data-loading="action(search)|hide" style="display: block">Search</span>
                </button>
            </div>
        </div>
    </div>
</div>
