<div class="alert alert-info p-2 text-center">
    The updated metadata from this page will be utilized to generate meta tags, page titles, and header tags specifically for the Product Page (Editor Page) on the website.
</div>
<div class="d-flex">
    <div class="nav nav-pills flex-column me-3" role="tablist" aria-orientation="vertical">
        {% for c in categories %}
            <a href="{{ url('admin_product_type_seo_meta_data', {id: productType.id, category: c.slug}) }}"
               class="nav-link {% if app.request.get('_route') in ['admin_product_type_seo_meta_data'] and c.slug == category.slug %}active{% endif %}"
               type="button">
                {{ c.name }}
            </a>
        {% endfor %}
    </div>
    <div class="tab-content flex-fill">
        {{ form_start(form) }}
        {% for key, metaData in form.children %}
            {% if key starts with 'meta_data_' %}
                <div class="card shadow-none mb-3 card-header-actions">
                    <div class="card-header h-100 text-dark">
                        {{ category.name }} - {{ metaData.vars.label }}
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-7">{{ form_row(metaData.children.title) }}</div>
                            <div class="col-5">{{ form_row(metaData.children.headerTag) }}</div>
                            <div class="col-12">{{ form_row(metaData.children.description) }}</div>
                            <div class="col-12">{{ form_row(metaData.children.keywords) }}</div>
                        </div>
                    </div>
                </div>
            {% endif %}
        {% endfor %}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
</div>